# Core Framework
fastapi==0.115.0
uvicorn[standard]==0.24.0
# Bumped from 2.5.0: anthropic's beta_tool/beta_async_tool decorators
# (src/copilot/tools.py) crash on 2.5.0 - TypeAdapter over a
# ValidateCallWrapper raises "unhashable type" - fixed by pydantic's own
# later 2.x internals. Verified against the full existing test suite
# (444 passed) before bumping; no other code in this repo depends on
# anything specific to 2.5.0.
pydantic==2.9.2
python-multipart==0.0.6
email-validator>=2.0.0

# ML & Data Processing
numpy==1.24.3
pandas==2.1.4
scikit-learn==1.3.2
xgboost==2.0.3
shap>=0.42.0
joblib>=1.2.0

# Dashboard & Visualization
streamlit==1.46.0
folium==0.19.0
streamlit-folium==0.24.0
plotly>=5.17.0

# Alert Providers
twilio>=8.0.0
python-dotenv>=1.0.0

# AI Copilot (src/copilot/) - Claude API, tool-calling Tool Runner beta
anthropic>=0.116.0

# Real OASIS CAP v1.2 XML export (src/api/routes/cap_export.py) - typed
# dataclass bindings for the exact CAP schema (identifier/sender/status/
# msgType/severity/urgency/certainty/area/...), serialized via xsdata
# rather than hand-built XML strings.
cap-tools>=3.1.0

# Remote sensing (src/hydrology/sentinel_processor.py needs `import ee` -
# environment.yml has this via conda-forge for the conda-based workflows,
# but ci.yml installs via plain pip, which never got this package)
earthengine-api>=0.1.400

# Testing
pytest==7.4.3
pytest-cov==4.1.0
pytest-mock==3.12.0
pytest-asyncio==0.21.1
httpx==0.25.1

# Utilities
python-dateutil==2.8.2
pyyaml==6.0.1
click==8.1.7
rich==13.7.0
requests>=2.31.0
gunicorn>=20.1.0
apscheduler>=3.10.0
starlette==0.38.6
