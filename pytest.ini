[pytest]
addopts = 
    --cov=src
    --cov-report=term-missing
    --cov-report=html
testpaths = tests
markers =
    slow: marks tests as slow (deselect with '-m "not slow"')
filterwarnings =
    ignore::DeprecationWarning
